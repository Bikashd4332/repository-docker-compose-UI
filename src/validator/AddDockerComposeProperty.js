/*
 * Functions adds the requested propertyName if valid to be added
 * else returns false.
 * @param propertyName String - the name of the property to add.
 * @param namespace [] - the path where propertyName will be added
 * @param dockerComposeConfigJson {} - the json format of yaml file.
 */
export default function(propertyName, dockerComposeConfigJson, namespace) {}
